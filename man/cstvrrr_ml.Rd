% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cstvrrr_ml.R
\name{cstvrrr_ml}
\alias{cstvrrr_ml}
\title{Estimation of CS-TV-RRRs Under Restrictions Using Maximum Likelihood}
\usage{
cstvrrr_ml(
  yvls,
  xvls,
  uvls,
  rrst,
  lnrs,
  ltrs,
  evar,
  arst = NULL,
  meth = "Nelder-Mead",
  lowr = -Inf,
  uppr = Inf,
  ctrl = list(),
  hess = FALSE
)
}
\arguments{
\item{yvls}{Matrix of dependent variables.}

\item{xvls}{Matrix of (reduced rank coefficients related) regressors.}

\item{uvls}{Matrix of (not reduced rank coefficients related) regressors.}

\item{rrst}{Specification of the restrictions on the row space
parameter matrix. Either \code{NULL} or a list of length 3. If list of
length 3, then first element restriction matrix, second element restriction
vector, third element vector of initial values for the free parameters.}

\item{lnrs}{Specification of the restrictions on the non-time-varying part
of the column space parameter matrix. Either \code{NULL} or a list. Must have
length 4 only if \code{ltrs} is \code{NULL} and \code{rrst} is \code{NULL};
otherwise must have length 3.
\itemize{
\item If list of length 4, then first element restriction matrix,
second element restriction vector, third element vector of initial values
for the free parameters, and fourth element initial value
for the row space parameter matrix.
\item If list of length 3, then first element restriction matrix,
second element restriction vector, third element vector of initial values
for the free parameters.
}}

\item{ltrs}{Specification of the restrictions on the time-varying part
of the column space parameter matrix. Either \code{NULL} or a list. Must have
length 4 if \code{rrst} is \code{NULL}, and length 3 otherwise.
\itemize{
\item If list of length 4, then first element restriction matrix,
second element restriction vector, third element vector of initial values
for the free parameters, and fourth element initial value
for the row space parameter matrix.
\item If list of length 3, then first element restriction matrix,
second element restriction vector, third element vector of initial values
for the free parameters.
}}

\item{evar}{Initial value for the covariance matrix of the error terms
in the measurement equation.}

\item{arst}{Specification of the restrictions on the coefficients of
the input vectors in measurement equation. If not \code{NULL}, \code{NULL} is returned.

Default is \code{NULL}.}

\item{meth}{Method to be used in ``stats::optim''.

Default is \code{Nelder-Mead}.}

\item{lowr}{Lower bound for certain methods in ``stats::optim''.

Default is `-Inf'.}

\item{uppr}{Upper bound for certain methods in ``stats::optim''.

Default is `Inf'.}

\item{ctrl}{List of control parameters for ``stats::optim''.

Default is `list()'.}

\item{hess}{Logical. Should ``stats::optim'' return a numerically
differentiated Hessian matrix?

Default is `FALSE'.}
}
\value{
Returns \code{NULL} if \code{arst} is not \code{NULL}, otherwise
a list of the following values:
\itemize{
\item \code{kalm}: Results obtained from Kalman smoother
\item \code{sime}: Estimated mean of initial state
\item \code{sicv}: Estimated covariance matrix of initial state
\item \code{evar}: Estimated covariance matrix of measurement equation errors
\item \code{sscf}: Estimated state transition matrix
\item \code{smea}: Estimated drift term in state equation
\item \code{secv}: Estimated covariance matrix of state equation errors
\item \code{augm}: Estimated coefficient matrix of input vectors in measurement
equation
\item \code{rfct}: Estimated row space parameter matrix
\item \code{lnfc}: Estimated non-time-varying part of column space parameter matrix
\item \code{ltfc}: Estimated time-varying part of column space parameter matrix
\item \code{rfur}: Estimated unrestricted row space parameter vector
\item \code{lnur}: Estimated unrestricted non-time-varying column space parameter
vector
\item \code{ltur}: Estimated unrestricted time-varying column space parameter vector
\item \code{nlik}: Negative of the log-likelihood (apart from constants) evaluated
at the optimal solutions found
\item \code{ltrm}: Restriction matrix on time-varying column space parameter matrix
\item \code{ltrv}: Restriction vector on time-varying column space parameter matrix
\item \code{conv}: Integer code indicating convergece. \code{0} indicates successful
completion.
}
}
\description{
Estimates an CS-TV-RRR under restrictions using maximum likelihood.
}
\references{
Brune, B., Scherrer, W. and Bura, E. (2022). A State-Space Approach to
Time-Varying Reduced-Rank Regression. Econometric Reviews 41, 895-917.

Veldhuis, S. and Wagner, M. (2026). Restrictions in a State Space Approach
to Time-Varying Vector Error Correction Models: Modelling Instabilities
in Long-Run Money Demand. Mimeo.
}
