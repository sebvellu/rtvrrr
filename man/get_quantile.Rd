% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_quantile.R
\name{get_quantile}
\alias{get_quantile}
\title{Extract Empirical Quantiles}
\usage{
get_quantile(smpl, perc)
}
\arguments{
\item{smpl}{Numeric vector containing the sample values.}

\item{perc}{Numeric value between 0 and 1 giving the desired
quantile level. For example, 0.5 returns the median.}
}
\value{
A numeric value equal to the sample quantile at the specified
probability level.
}
\description{
This function returns empirical quantiles from a numeric sample.
It uses a partial sort to avoid sorting the full vector when only
one order statistic is needed.
}
\details{
The function computes

q = \code{floor(perc * length(smpl))}

and extracts the q-th order statistic using sort with the partial
argument, which performs only the work needed to find the selected
element.
}
\examples{
set.seed(1)
x <- rnorm(100)
get_quantile(x, 0.5)
get_quantile(x, 0.9)

}
\keyword{internal}
